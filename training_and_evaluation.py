# -*- coding: utf-8 -*-
"""training_and_evaluation.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vT0iBcs8zWrQzJmwXWI57k9bsuNqsI6g
"""

from xgboost import XGBRegressor
from sklearn.metrics import mean_squared_error, mean_absolute_error
import numpy as np
import pickle

xgb_bat = XGBRegressor(
    n_estimators=800,
    learning_rate=0.03,
    max_depth=6,
    subsample=0.8,
    colsample_bytree=0.8,
    reg_lambda=1,
    reg_alpha=0.5,
    random_state=42,
    tree_method="hist",
    early_stopping_rounds=50
)

xgb_bat.fit(
    X_train_bat,
    y_train_bat,
    eval_set=[(X_val_bat, y_val_bat)],
    verbose=False
)

xgb_bowl = XGBRegressor(
    n_estimators=800,
    learning_rate=0.03,
    max_depth=6,
    subsample=0.8,
    colsample_bytree=0.8,
    reg_lambda=1,
    reg_alpha=0.5,
    random_state=42,
    tree_method="hist",
    early_stopping_rounds=50
)

xgb_bowl.fit(
    X_train_bowl,
    y_train_bowl,
    eval_set=[(X_val_bowl, y_val_bowl)],
    verbose=False
)

def evaluate(y_true, y_pred):
    rmse = np.sqrt(mean_squared_error(y_true, y_pred))
    mae = mean_absolute_error(y_true, y_pred)
    return round(rmse,2), round(mae,2)

print("------ FINAL MODEL PERFORMANCE ------")

bat_pred = xgb_bat.predict(X_test_bat)
bowl_pred = xgb_bowl.predict(X_test_bowl)

print("\nBatsman Runs Prediction")
print("RMSE, MAE:", evaluate(y_test_bat, bat_pred))

print("\nBowler Wickets Prediction")
print("RMSE, MAE:", evaluate(y_test_bowl, bowl_pred))

pickle.dump(xgb_bat, open("best_batsman_model.pkl","wb"))
pickle.dump(xgb_bowl, open("best_bowler_model.pkl","wb"))

print("\nBest models saved successfully.")